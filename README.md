# Reactive vs Traditional IoT Dashboard

A Python project that compares reactive and traditional programming using a live Streamlit dashboard.

## Requirements

- Python 3.10 or newer

## Install

Install the project dependencies:

```bash
pip install streamlit altair pandas reactivex
```

## Run

Start the dashboard from the project root:

```bash
streamlit run dashboard/dashboard_app.py
```


